name: Test Action
on:
  push:
    branches: [test/only-comment-threshold]

jobs:
  test:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: 'actions/checkout@v3'
      - name: Create commit comment
        uses: hugofpaiva/gzoltar-feedback-action@main